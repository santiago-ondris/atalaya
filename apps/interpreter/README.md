# Interpreter

Servicio interno FastAPI para interpretar errores mediante OpenRouter.

- `GET /health`: confirma que el proceso HTTP responde.
- `GET /ready`: confirma que la configuración necesaria está disponible.

La dependencia de OpenRouter se incorporará en el Sprint 2.

